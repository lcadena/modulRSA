import java.math.BigInt;
import java.util.Scanner;


Public class PublicKey
{
  constructor( e, N) {
    this.e = BigInt e;
    this.N = BigInt N;
  }
}

Public class PrivateKey
{
    constructor( d, n)
        this.d = BigInt d;
        this.n = BigInt n;
    }



   function  encrypt ( m, n){

        var this.n              = n;
        const this.m            = BigInt m;
        const                   = BigInt math.pow(m,e)%n
        console.log(operation);

        return operation;

    }

    function  decrypt (BigInt cripto){

            bigint  m = this.m;
            bigint  operation = math.pow(c,d)%n
                        console.log(operation);

            return operation;
    }

